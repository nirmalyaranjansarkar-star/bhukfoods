import React from 'react';

const Chatbot: React.FC = () => {
  // This component has been disabled as per previous requests to ensure
  // it does not interfere with the application's functionality.
  return null;
};

export default Chatbot;